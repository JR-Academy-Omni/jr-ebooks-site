# Design Language: jr-brand（默认）

**权威 spec**：`jr-academy-brand/DESIGN.md` v4.2 + Brand Board — 本 repo 的 `../tokens.css` 是其副本，**不加载任何 designs/*.css 即为本语言**。

- **何时用**：一切匠人学院自有发行的书（默认值，不确定就用它）
- **核心**：暖白 `#FFFCF6` + 黑 `#0D0F12` 主 CTA + 红 `#FF4D4F` 只做编号/强调 + 紫做 AI/学习语义
- **完整规则**：见 repo 根 `DESIGN.md` §1（底座）
